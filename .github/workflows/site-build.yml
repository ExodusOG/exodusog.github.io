name: Site build (no sitemap automation)

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No automated sitemap/dataset generation
        run: echo "No-op: sitemap is static; no scripts run."
