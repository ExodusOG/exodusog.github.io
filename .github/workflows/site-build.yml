name: Deploy site content

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: No-op (content deploy handled by GitHub Pages)
        run: echo "Deploy triggered on push."
